// Client-side config. STADIA_API_KEY is a Stadia Maps API key restricted to this site's
// domain(s) in the Stadia dashboard (their intended pattern for client-side keys) — see
// README "Setup" for the signup + domain-restriction step. Left blank, the map front falls
// back to the paper texture + lockup (v1.9 §4).
export const STADIA_API_KEY = '';
